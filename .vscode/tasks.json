{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Full Rebuild: Android + Docker Compose",
      "type": "shell",
      "command": "cd frontend; if ($?) { npm run build }; if ($?) { npx cap sync android }; if ($?) { cd .. }; if ($?) { docker compose down -v }; if ($?) { docker compose up -d --build }",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": false
      },
      "presentation": {
        "reveal": "always",
        "panel": "shared",
        "clear": true
      },
      "problemMatcher": []
    },
    {
      "label": "Docker Compose: down then up -d --build",
      "type": "shell",
      "command": "docker compose down; docker compose up -d --build",
      "options": {
        "cwd": "${workspaceFolder}",
        "env": {}
      },
      "group": {
        "kind": "build",
        "isDefault": false
      },
      "presentation": {
        "reveal": "always",
        "panel": "shared",
        "clear": true
      }
    },
    {
      "label": "Seed test data for @joey",
      "type": "shell",
      "command": "docker compose exec django_api python manage.py seed_test_data",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "presentation": {
        "reveal": "always",
        "panel": "shared",
        "clear": true
      },
      "problemMatcher": []
    }
  ]
}
